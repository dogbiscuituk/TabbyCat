float
  t = timeValue,
  x = position.x,
  y = position.y,
  z = position.z,
  r = 0,
  g = 0,
  b = 0;

switch (axesUsed)
{
  case 2: y = x; x = 0; break;
  case 4: z = x; x = 0; break;
  case 5: z = x; x = y; y = 0; break;
  case 6: z = y; y = x; x = 0; break;
}

// Begin Formula


// End Formula

gl_Position = projection * cameraView * transform * vec4(x, y, z, 1);
colour = vec3(r, g, b);