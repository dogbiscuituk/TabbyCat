float
  t = timeValue,
  x = position.x,
  y = position.y,
  z = position.z,
  r = 0,
  g = 0,
  b = 0;

// Begin Formula


// End Formula

gl_Position = projection * cameraView * transform * vec4(x, y, z, 1);
colour = vec3(r, g, b);